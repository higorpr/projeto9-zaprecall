import styled from "styled-components";

function Footer() {
    return (
        <FooterContainer>
            TESTE PÉ
        </FooterContainer>
    )
}

export default Footer;

// Styled Components

const FooterContainer = styled.div`

`;